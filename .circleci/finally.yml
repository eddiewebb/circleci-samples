version: 2.1
orbs:
  node: circleci/node@4.3.0 #conflict with A&B

jobs:
  e2e-tests:
    executor: node/default
    steps:
      - run: echo "boop"
  publish-lambda:
    executor: node/default
    steps:
      - run: echo "boop"
  publish-docker:
    executor: node/default
    steps:
      - run: echo "boop"
workflows:
  finally-workflow:
    jobs:
      - e2e-tests  
      - publish-lambda:
          requires:
            - e2e-tests        
      - publish-docker:
          requires:
            - e2e-tests                                                                                                                                                                                    
