version: 2.1

executors:

  latest:
    docker:
    - image: cimg/base:stable

jobs:
  only:
    executor: latest
    parallelism: 2
    steps:
      - run: set
      - run: |
          if [ $CIRCLE_NODE_INDEX -gt 0 ]; then
            echo "USING MIRROR"
            cat repo.list > sudo tee /etc/apt/sources.list
          fi
      - run: |
          sudo apt update
      - run: |
          sudo apt install -y php nginx python3
          
          




  
workflows:
  test-deploy:
    jobs:
      - only:
          name: do stuff