version: 2.1

executors:

  latest:
    macos:
      xcode: 15.4.0

jobs:
  only:
    executor: latest
    steps:
      - run: set
      - run: |
          if [ "${DEBUG}" == "true" ] ;then
            echo "DEBUG"
          fi
          
          




  
workflows:
  test-deploy:
    jobs:
      - only:
          name: do stuff