version: 2.1

workflows:
  version: 2
  workspace:
    jobs:
      - primary_docker_dns2

jobs:


  primary_docker_dns2:
    docker: 
      - image: circleci/node:10
        command: sudo sh -c 'echo nameserver 8.8.8.8 >> /etc/resolv.conf && /bin/bash'
    steps:
      - run: cat /etc/resolv.conf

