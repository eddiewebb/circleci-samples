version: 2.1

executors:
  default:
    docker:
      - image: cimg/node:14.18.2

jobs:
  keep-going:
    executor: default
    resource_class: small
    parallelism: 4
    steps:
      - run: sleep $[ ( $RANDOM % 20 )  + 1 ]s
      




  
workflows:
  version: 2
  test-deploy:
    jobs:
      - keep-going