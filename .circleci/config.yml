version: 2.1

workflows:
  default:
    jobs:
      - build

jobs:
  build:
    docker:
      images: 
        - cimg/base:stable
    steps:
      - checkout
      - run: 
          name: Hello World
          command: |
            echo "Hello World"
            echo "and again"