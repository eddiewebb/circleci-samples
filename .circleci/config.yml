version: 2.1

executors:
  default:
    docker:
      - image: cimg/node:14.18.2

jobs:
  job-1:
    executor: default
    resource_class: small
    steps:
      - run: whoami
      - run: ls -la /root
      - run: ls -la /root/.npm
  
workflows:
  version: 2
  test-deploy:
    jobs:
      - job-1