version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - setup_remote_docker
      - run: |
          echo ${CIRCLE_BRANCH}
          TRAVIS_BRANCH=${CIRCLE_BRANCH}
          docker run -e "TRAVIS_BRANCH=${CIRCLE_BRANCH}" eddiewebb/echo:latest