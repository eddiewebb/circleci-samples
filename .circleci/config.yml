version: 2.1

executors:
  default:
    docker:
      - image: cimg/node:14.18.2

jobs:
  keep-going:
    executor: default
    resource_class: small
    steps:
      - checkout
      - run: bash rickroll.sh

  
workflows:
  version: 2
  test-deploy:
    jobs:
      - keep-going