version: 2.1

executors:
  base:
    docker:
      - image: cimg/base:2021.07

jobs:
  try-it:
    executor: base
    resource_class: small
    steps:
      - run: echo "contents" > file1.txt
      - run: echo "contents" > file2.txt
      - store_artifacts:
          name: Storing Artifacts
          path: 'file*.txt'
  
workflows:
  version: 2
  test-deploy:
    jobs:
      - try-it