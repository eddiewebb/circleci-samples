version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - setup_remote_docker
      - run: |
          echo ${KEY} > key.pem
          head -2 key.pem
          ssh-keygen -y -e -f key.pem