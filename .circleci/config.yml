version: 2.1

orbs:
  jira: circleci/jira@1.0.0
  hello-build: circleci/hello-build@0.0.13


workflows:
  build-deploy:
    jobs:
      - hello-build/hello-build:
          post-steps:
          - jira/notify