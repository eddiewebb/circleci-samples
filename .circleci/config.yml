version: 2.1
orbs:
  stats: eddiewebb/stats@dev:local
workflows:
  stress_stats:
    jobs:
      - stats/with_stats:
          job_steps:
            - run: sudo apt-get update && sudo apt-get install -y stress
            - run: stress --vm 4 --timeout 60
            - stats/credits_used
            - cat /tmp/build-info.json