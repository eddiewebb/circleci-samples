version: 2

workflows:
  version: 2
  storage-test:
    jobs:
      - many


jobs:
  many:
    parallelism: 5
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - hostname
      - echo $CIRCLE_NODE_TOTAL
      - echo $CIRCLE_NODE_INDEX
      - set
