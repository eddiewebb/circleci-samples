machine:
  php:
    version: 7.1.9
  services:
    - docker


test:
  override:
    - exit 1 # failing test
  post:
    - exit 0 # successful things


deployment:
  testing:
    branch: /.*/
    commands:
      - echo "Deployed!"
